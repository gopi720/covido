@Library('sharedlib1') _
pipeline{
    agent any
    tools{
        maven "maven"
    }
    stages{
        stage("git clone"){
            steps{
                gitcheckout()
            }
        }
        stage("build project"){
            steps{
                mavenbuild()
            }
        }
    }
}